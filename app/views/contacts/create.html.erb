<div>
  <% flash.each do |key, value| %>
    <div>
      <a href="#" data-dismiss="alert">×</a>
        <%= value %>
    </div>
  <% end %>
  <%= flash[:notice]%>
  <%= flash[:error]%>
</div>

<%= debug controller.session %>